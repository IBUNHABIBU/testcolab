import React from 'react';

const Admin = () => (
  <div className="admin">
    <h2>Admin Panel</h2>
    <div className="admin__buttons">
      <button className="btn skills" type="submit">Add Skills</button>
      <button className="btn project" type="submit">Add Project</button>
    </div>
  </div>
);

export default Admin;
